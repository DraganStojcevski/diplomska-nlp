# Local Model Artifacts

Fine-tuned model binaries are not committed to GitHub because they are large.

Expected local folders:

- `models/distilbert-finetuned/`
- `models/bert-large-finetuned/`

The Streamlit app can still show saved experiment results without these folders.
For live predictions with fine-tuned models, place the trained model files in the folders above.
